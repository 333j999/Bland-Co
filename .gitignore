# Secrets / env — never commit (public repo)
.env
.env.*
!.env.example
admin-config.json

# Convex
.convex/
convex/_generated/

# Vercel / build
.vercel/
node_modules/

# Local runtime data (seed lives in data/, written copies stay local)
# (data/*.json are tracked as seed; remove that tracking if it ever holds real customer data)

# OS
.DS_Store
Thumbs.db
